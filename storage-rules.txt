rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Este sistema CETEC não usa Storage por enquanto.
    // Mantido fechado por segurança.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
